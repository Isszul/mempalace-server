services:
  mempalace:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - palace:/palace
    environment:
      MEMPALACE_PALACE_PATH: /palace
      PORT: "8080"
    restart: unless-stopped

volumes:
  palace:
